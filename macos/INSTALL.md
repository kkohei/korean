# 日韓翻訳 — インストール手順（受け取った方へ）

Mac のメニューバーに常駐する、日本語 ⇄ 韓国語の翻訳アプリです。

## 1. インストール

1. `KoreanTranslator.dmg` をダブルクリックで開く
2. 出てきた **アプリのアイコンを「Applications」フォルダへドラッグ**
3. 「アプリケーション」フォルダから **日韓翻訳** を起動

## 2. 初回起動で「開けません」と出たら

このアプリは個人配布のため、初回だけ macOS に確認を求められることがあります。

- `日韓翻訳`（KoreanTranslator）を **右クリック → 開く** → ダイアログで **開く** を押す

それでも開けない場合は、ターミナルで以下を実行してください：

```bash
xattr -dr com.apple.quarantine /Applications/KoreanTranslator.app
```

## 3. APIキーの設定（最初の1回だけ）

翻訳には Anthropic の APIキーが必要です。

1. https://console.anthropic.com を開いてサインアップ
2. **Billing** でクレジットを購入（最低 $5〜。1回の翻訳は数円程度）
3. **API keys → Create Key** で `sk-ant-...` を発行してコピー
4. メニューバーの緑の **「韓」** をクリック → **⚙設定** にキーを貼り付けて「保存」

## 4. 使い方

- メニューバーの **「韓」** をクリックでウィンドウ開閉
- ウィンドウはデスクトップの好きな場所へドラッグ移動できます
- 上部の **⇄** で翻訳方向（日→韓 / 韓→日）を切替
- 文章を入力して **⌘ + Return**（または「翻訳」ボタン）
- ⚙設定で文字色や背景の濃さも調整できます

困ったことがあれば配布元へご連絡ください。
